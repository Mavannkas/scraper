extends layout

block content
    .container.d-flex.justify-content-center.bg-light
        .row.text-center
            .col-md-12 
                h1 Oops! 
                h2 #{error}
                img(src="https://http.cat/"+code).img-fluid